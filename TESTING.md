For everyone

1. Open the calendar – current month is shown

How tested:
Opened index.html in the browser.
Verified that the displayed month and year match the system’s current date.
Confirmed that the grid updates automatically based on the current date.

2. October 2024 layout

How tested:
Selected October 2024 using the month and year selectors.

Verified:

Grid has 5 rows × 7 columns.

First row: Tuesday October 1 – Saturday October 5.
Sunday and Monday appear as empty boxes.

Last row: Sunday October 27 – Thursday October 31.
Friday and Saturday appear as empty boxes.

3. Special days – October 2024

How tested:
In October 2024:

October 8 shows Ada Lovelace Day.

October 25 shows World Lemur Day.

Verified visually in the UI (special styling + event name displayed).

4. Special days – October 2020

How tested:
Opened October 2020 in the UI.

Verified:

October 13 shows Ada Lovelace Day.

October 30 shows World Lemur Day.

5. Special days – May 2030

How tested:
Opened May 2030 in the UI.

Verified:

May 11 shows International Binturong Day.

6. Correct month start/end and padding

Manually checked the following months in the UI:

2024-12
Starts Sunday (no empty days before)
Ends Tuesday (4 empty days after)

2025-02
Starts Saturday (6 empty days before)
Ends Friday (1 empty day after)

2025-05
Starts Thursday (4 empty days before)
Ends Saturday (no empty days after)

2026-02
Starts Sunday (no empty days before)
Ends Saturday (no empty days after)

Verified that:

No extra rows are created.

No missing or additional days.

Padding boxes only fill required positions.

7. Previous / Next buttons

How tested:

Clicked Previous and Next buttons repeatedly.

Navigated across year boundaries (e.g., December → January).

Verified month and year update correctly.

Verified selectors stay in sync.

Verified no “undefined”, “null”, “NaN” or empty strings appear.

Calendar days always render correctly.

8. Jump to specific month + year

How tested:

Used both dropdown selectors:

Changed month only.

Changed year only.

Changed both.

Verified:

Calendar updates immediately.

Title updates correctly.

Grid renders correct days.

9. UI generated by DOM manipulation & dynamic data

How tested:

Confirmed days are generated via GetDaysOfMonth().

Confirmed special days are injected using getSpecialDays().

No hardcoded calendar days exist in HTML.

Verified that adding a new special day entry to days.json automatically appears in the UI without changing other code.

10. Accessibility (Lighthouse)

How tested:

Opened Chrome DevTools.

Ran Lighthouse audit.

Verified Accessibility score = 100.

11. Unit tests

Unit tests in tests/logic.test.js

Tested:

GetDaysOfMonth()

Correct number of days (normal year)

Correct number of days (leap year)

Returns array

getSpecialDays()

Correct detection of Ada Lovelace Day

Correct detection of World Lemur Day

Run with:

npm test

All tests pass.

For groups of at least 2 12. Generate days.ics file

How tested:

Ran:

node generate-ics.js

Verified:

days.ics file is created.

File contains events from 2020 to 2030 inclusive.

One event per commemorative day per year.

No recurring events (no RRULE).

Events use DTSTART;VALUE=DATE and DTEND;VALUE=DATE (whole-day events only).

13. Import into Google Calendar

How tested:

Imported days.ics into Google Calendar.

Verified same dates as shown in web UI:

Ada Lovelace Day

World Lemur Day

International Binturong Day

Other September events

Verified events appear as all-day events (no start/end time shown).

14. Shared date logic (Web + iCal)

How tested:

Confirmed both:

Web UI

generate-ics.js

Use the same logic functions:

GetDaysOfMonth()

getSpecialDays()

This ensures consistency between UI rendering and generated iCal file.
